# cnn_pneumonia_detection
Deep learning pipeline that classifies X-rays as Normal/Pneumonia using a CNN based on ResNet50 with transfer learning, explains predictions using Grad-CAM heatmaps and evaluates the model with clinically meaningful metrics
